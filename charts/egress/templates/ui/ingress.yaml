{{- if and .Values.global.ingress.enabled .Values.ui.enabled .Values.ui.ingress.enabled -}}
{{- $host := .Values.ui.ingress.host | default (printf "egress.%s" .Values.global.ingress.host) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "egress.fullname" . }}-ui
  labels:
    {{- include "egress.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.global.ingress.certClusterIssuer }}
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
spec:
  ingressClassName: {{ .Values.global.ingress.className }}
  {{- if .Values.global.ingress.tls }}
  tls:
    - hosts:
        - {{ $host }}
      secretName: egress-tls
  {{- end }}
  rules:
    - host: {{ $host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: egress-ui
                port:
                  number: 80
{{- end }}
