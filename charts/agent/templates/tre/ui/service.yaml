apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent.fullname" . }}-tre-ui
  labels:
    {{- include "agent.treLabels" . | nindent 4 }}
spec:
  type: {{ .Values.tre.ui.service.type }}
  ports:
    - port: {{ .Values.tre.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent.treUiSelectorLabels" . | nindent 4 }}