{{- if .Values.tre.api.ingress.enabled -}}
{{- $fullName := include "agent.fullname" . -}}
{{- $svcPort := .Values.tre.api.service.port -}}
{{- if and .Values.tre.api.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.tre.api.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.tre.api.ingress.annotations "kubernetes.io/ingress.class" .Values.tre.api.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-api
  labels:
    {{- include "agent.treApiLabels" . | nindent 4 }}
  {{- with .Values.tre.api.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.tre.api.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.tre.api.ingress.className }}
  {{- end }}
  {{- if .Values.tre.api.ingress.tls }}
  tls:
    {{- range .Values.tre.api.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.tre.api.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-tre-api
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-tre-api
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
    - host: {{ .Values.tre.api.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-tre-api
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-tre-api
              servicePort: {{ $svcPort }}
              {{- end }}
{{- end }}
