{{- if .Values.egress.api.ingress.enabled -}}
{{- $fullName := include "agent.fullname" . -}}
{{- $svcPort := .Values.egress.api.service.port -}}
{{- if and .Values.egress.api.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.egress.api.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.egress.api.ingress.annotations "kubernetes.io/ingress.class" .Values.egress.api.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-api
  labels:
    {{- include "agent.egressApiLabels" . | nindent 4 }}
  {{- with .Values.egress.api.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.egress.api.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.egress.api.ingress.className }}
  {{- end }}
  {{- if .Values.egress.api.ingress.tls }}
  tls:
    {{- range .Values.egress.api.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.egress.api.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-egress-api
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-egress-api
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
    - host: {{ .Values.egress.api.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-egress-api
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-egress-api
              servicePort: {{ $svcPort }}
              {{- end }}
{{- end }}
