{{- if .Values.egress.ui.ingress.enabled -}}
{{- $fullName := include "agent.fullname" . -}}
{{- $svcPort := .Values.egress.ui.service.port -}}
{{- if and .Values.egress.ui.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.egress.ui.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.egress.ui.ingress.annotations "kubernetes.io/ingress.class" .Values.egress.ui.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-ui
  labels:
    {{- include "agent.egressUiLabels" . | nindent 4 }}
  {{- with .Values.egress.ui.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.egress.ui.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.egress.ui.ingress.className }}
  {{- end }}
  {{- if .Values.egress.ui.ingress.tls }}
  tls:
    {{- range .Values.egress.ui.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.egress.ui.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-egress-ui
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-egress-ui
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
    - host: {{ .Values.egress.ui.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-egress-ui
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-egress-ui
              servicePort: {{ $svcPort }}
              {{- end }}
{{- end }}
