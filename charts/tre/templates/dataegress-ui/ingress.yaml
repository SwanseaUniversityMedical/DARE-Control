{{- if .Values.dataEgress.ui.ingress.enabled -}}
{{- $fullName := include "tre.fullname" . -}}
{{- $svcPort := .Values.dataEgress.ui.service.port -}}
{{- if and .Values.dataEgress.ui.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.dataEgress.ui.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.dataEgress.ui.ingress.annotations "kubernetes.io/ingress.class" .Values.dataEgress.ui.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-data-egress-ui
  labels:
    {{- include "tre.labels" . | nindent 4 }}
    component: "data-egress-ui"
  {{- with .Values.dataEgress.ui.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.dataEgress.ui.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.dataEgress.ui.ingress.className }}
  {{- end }}
  {{- if .Values.dataEgress.ui.ingress.tls }}
  tls:
    {{- range .Values.dataEgress.ui.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.dataEgress.ui.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-data-egress-ui
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-data-egress-ui
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
    - host: {{ .Values.dataEgress.ui.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-data-egress-ui
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-data-egress-ui
              servicePort: {{ $svcPort }}
              {{- end }}
{{- end }}