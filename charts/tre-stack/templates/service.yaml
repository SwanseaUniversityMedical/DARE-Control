apiVersion: v1
kind: Service
metadata:
  name: {{ include "tre-stack.fullname" . }}
  labels:
    {{- include "tre-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tre-stack.selectorLabels" . | nindent 4 }}
