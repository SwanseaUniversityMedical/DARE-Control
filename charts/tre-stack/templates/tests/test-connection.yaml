apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tre-stack.fullname" . }}-test-connection"
  labels:
    {{- include "tre-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tre-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
