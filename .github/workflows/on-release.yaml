name: Release

on:
  push:
    branches:
      - main


permissions:
  # read needed for changes detection
  # write needed to author a Github release
  contents: write
  
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  changes:
    # Detect which files have been changed on this PR's full history
    uses: SwanseaUniversityMedical/workflows/.github/workflows/changes-release.yaml@v5.0.3
    with:
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      filters: |
        api-container:
          - './.github/workflows/on-release.yaml'
          - 'containers/api/**'
          - 'src/DARE-API/**'
          - 'src/BL/**'
        ui-container:
          - './.github/workflows/on-release.yaml'
          - 'src/DARE-FrontEnd/**'
          - 'src/BL/**'
        egress-api-container:
          - './.github/workflows/on-release.yaml'
          - 'src/Data-Egress-API/**'
          - 'src/BL/**'
        egress-ui-container:
          - './.github/workflows/on-release.yaml'
          - 'src/Data-Egress-UI/**'
          - 'src/BL/**'
        tre-api-container:
          - './.github/workflows/on-release.yaml'
          - 'src/TRE-API/**'
          - 'src/BL/**'
        tre-ui-container:
          - './.github/workflows/on-release.yaml'
          - 'src/TRE-UI/**'
          - 'src/BL/**'
        tre-hasura-container:
          - './.github/workflows/on-release.yaml'
          - 'src/Tre-Hasura/**'
          - 'src/BL/**'
        chart:
          - './.github/workflows/on-release.yaml'
          - 'charts/**'

    secrets:
      slack-token: ${{ secrets.SLACK_TOKEN }}

  api-container:
    # Build the DARE api container and push it as :rc-1.0.0
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).api-container == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/build-and-test-release-container.yaml@v5.0.3
    with:
      job-name: api-container
      registry: ${{ vars.HARBOR_REGISTRY }}
      registry-user: ${{ vars.HARBOR_USER }}
      registry-repo: ${{ vars.HARBOR_PROJECT }}/${{ vars.HARBOR_REPO }}-main-api
      release-tag-format: 'DARE-Control-API-Container-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      build-file: src/DARE-API/Dockerfile
      build-context: src/
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}

  ui-container:
    # Build the DARE ui container and push it as :rc-1.0.0
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).ui-container == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/build-and-test-release-container.yaml@v5.0.3
    with:
      job-name: ui-container
      registry: ${{ vars.HARBOR_REGISTRY }}
      registry-user: ${{ vars.HARBOR_USER }}
      registry-repo: ${{ vars.HARBOR_PROJECT }}/${{ vars.HARBOR_REPO }}-main-ui
      release-tag-format: 'DARE-Control-UI-Container-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      build-file: src/DARE-FrontEnd/Dockerfile
      build-context: src/
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}

  egress-api-container:
    # Build the egress api container and push it as :rc-1.0.0
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).egress-api-container == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/build-and-test-release-container.yaml@v5.0.3
    with:
      job-name: egress-api-container
      registry: ${{ vars.HARBOR_REGISTRY }}
      registry-user: ${{ vars.HARBOR_USER }}
      registry-repo: ${{ vars.HARBOR_PROJECT }}/${{ vars.HARBOR_REPO }}-egress-api
      release-tag-format: 'DARE-Egress-API-Container-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      build-file: src/Data-Egress-API/Dockerfile
      build-context: src/
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}

  egress-ui-container:
    # Build the egress ui container and push it as :rc-1.0.0
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).egress-ui-container == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/build-and-test-release-container.yaml@v5.0.3
    with:
      job-name: egress-ui-container
      registry: ${{ vars.HARBOR_REGISTRY }}
      registry-user: ${{ vars.HARBOR_USER }}
      registry-repo: ${{ vars.HARBOR_PROJECT }}/${{ vars.HARBOR_REPO }}-egress-ui
      release-tag-format: 'DARE-Egress-UI-Container-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      build-file: src/Data-Egress-UI/Dockerfile
      build-context: src/
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}

  tre-api-container:
    # Build the tre api container and push it as :rc-1.0.0
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).tre-api-container == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/build-and-test-release-container.yaml@v5.0.3
    with:
      job-name: tre-api-container
      registry: ${{ vars.HARBOR_REGISTRY }}
      registry-user: ${{ vars.HARBOR_USER }}
      registry-repo: ${{ vars.HARBOR_PROJECT }}/${{ vars.HARBOR_REPO }}-tre-api
      release-tag-format: 'DARE-TRE-API-Container-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      build-file: src/TRE-API/Dockerfile
      build-context: src/
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}

  tre-ui-container:
    # Build the tre ui container and push it as :rc-1.0.0
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).tre-ui-container == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/build-and-test-release-container.yaml@v5.0.3
    with:
      job-name: tre-ui-container
      registry: ${{ vars.HARBOR_REGISTRY }}
      registry-user: ${{ vars.HARBOR_USER }}
      registry-repo: ${{ vars.HARBOR_PROJECT }}/${{ vars.HARBOR_REPO }}-tre-ui
      release-tag-format: 'DARE-TRE-UI-Container-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      build-file: src/TRE-UI/Dockerfile
      build-context: src/
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}
      
  tre-hasura-container:
    # Build the tre hasura container and push it as :rc-1.0.0
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).tre-hasura-container == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/build-and-test-release-container.yaml@v5.0.3
    with:
      job-name: tre-hasura-container
      registry: ${{ vars.HARBOR_REGISTRY }}
      registry-user: ${{ vars.HARBOR_USER }}
      registry-repo: ${{ vars.HARBOR_PROJECT }}/${{ vars.HARBOR_REPO }}-tre-hasura
      release-tag-format: 'DARE-TRE-HASURA-Container-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      build-file: src/Tre-Hasura/Dockerfile
      build-context: src/
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}
      
  chart:
    # Build the chart and push it as x.y.z-rc
    needs: changes
    if: fromJSON(needs.changes.outputs.changes).chart == 'true'
    uses: SwanseaUniversityMedical/workflows/.github/workflows/on-release-chart.yaml@v5.0.3
    with:
      job-name: chart
      chart: charts/control
      chart-registry: ${{ vars.HARBOR_REGISTRY }}
      chart-registry-user: ${{ vars.HARBOR_USER }}
      chart-registry-project: ${{ vars.HARBOR_PROJECT }}
      chart-registry-repo: ${{ vars.HARBOR_REPO }}
      release-tag-format: 'DARE-Control-Helm-${version}'
      cosign-public-key: ${{ vars.COSIGN_PUBLIC_KEY }}
      slack-channel: ${{ vars.SLACK_CHANNEL }}
      test-command: |
        helm template $CHART --values charts/control/values.yaml
    secrets:
      cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}
      cosign-password: ${{ secrets.COSIGN_PASSWORD }}
      chart-registry-token: ${{ secrets.HARBOR_TOKEN }}
      slack-token: ${{ secrets.SLACK_TOKEN }}
