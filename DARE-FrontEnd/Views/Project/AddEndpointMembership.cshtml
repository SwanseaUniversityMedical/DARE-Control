@model BL.Models.DTO.ProjectEndpoint
<div class="row">
    <div class="col-sm-12">
        <h1>Add User to Project</h1>

        

        

        @using (Html.BeginForm("AddEndpointMembership", "Project", FormMethod.Post, new { id = "frmMain" }))
        {
            @Html.AntiForgeryToken()


            <div class="form-group">
                @{
                    @Html.LabelFor(m => m.EndpointId, "Select the Endpoint")
                    @Html.DropDownListFor(m => m.EndpointId, Model.EndpointItemList, new {@class = "form-control show-tick noBottomMargin"})

                }

                @Html.ValidationMessageFor(m => m.ProjectId, "Select Endpoint")

            </div>
            <div class="form-group">
                @{
                    @Html.LabelFor(m => m.ProjectId, "Select the project name")
                    @Html.DropDownListFor(m => m.ProjectId, Model.ProjectItemList, new {@class = "form-control show-tick noBottomMargin"})
                    
                }

                @Html.ValidationMessageFor(m => m.ProjectId, "Select Project")

            </div>

            



            <div class="hero-block-1">
                <button id="btnSubmit" type="button" class="btn btn-primary"><i class="fas fa-paper-plane"></i>Add Endpoint Membership</button>
            </div>
        }

    </div>
</div>